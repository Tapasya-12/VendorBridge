{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed": "cd ../lib/db && pnpm exec tsx src/seed.ts",
    "seed:clear": "tsx ./src/seed-management.ts clear",
    "seed:reset": "tsx ./src/seed-management.ts reset",
    "seed:check": "tsx ./src/seed-management.ts check"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
